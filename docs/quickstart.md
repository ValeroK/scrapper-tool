# Quickstart

> *Stub — populated in M9 (v0.1.0 release). The README's "Quickstart (5 minutes)" section is the current source.*

See the [README quickstart](../README.md#quickstart-5-minutes) for the working snippet.

This page will expand to cover:

1. Install + first request via `vendor_client()`.
2. Pattern B example: parse a schema.org Product page with `extruct`.
3. Pattern C example: schema.org microdata price extraction.
4. Anti-bot ladder example: `use_curl_cffi=True` + log the winning profile.
5. Pattern D example (with `[hostile]` extra): Cloudflare Turnstile auto-solve.
6. Fixture-replay testing pattern.
